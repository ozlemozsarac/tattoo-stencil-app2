allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
```

**Commit message:** `Simplify android/build.gradle`

---

## 📋 Kontrol Listesi

GitHub'da şu dosyaların var olduğundan emin ol:
```
✅ android/settings.gradle (YENİ içerikle)
✅ android/build.gradle (basitleştirilmiş)
✅ android/gradle.properties
✅ android/app/build.gradle (compileSdk 35)
✅ android/app/src/main/AndroidManifest.xml (icon kaldırılmış)
